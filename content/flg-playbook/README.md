/**
 * Source of truth for the new Founder Led Growth playbook copy.
 *
 * - `source/*.txt` — extracted from FLG Lead Magnet.docx (SR)
 * - `curriculum.json` — lesson order, slugs, phases
 * - Design pass: one lesson at a time in `src/data/lessons/lesson-XX-*.ts`
 *
 * Rebuild prose stubs: `node scripts/build-flg-lessons-from-source.mjs`
 * (Overwrites lesson TS files — only run before a lesson has been designed.)
 */
